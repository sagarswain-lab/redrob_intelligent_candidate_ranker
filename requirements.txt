# --- Core ranking pipeline (rank.py) ---
# TfidfVectorizer / cosine_similarity come from scikit-learn; numpy/scipy are its dependencies.
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10

# --- Sandbox UI (app.py) ---
# NOTE: gradio is NOT listed here because HuggingFace Spaces installs it
# automatically based on the sdk_version in README.md (currently 4.44.0).
# Adding gradio here would cause a version conflict and a build error.
pandas>=2.0
