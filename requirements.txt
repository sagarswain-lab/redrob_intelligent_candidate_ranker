# --- Core ranking pipeline (rank.py) ---
# TfidfVectorizer / cosine_similarity come from scikit-learn; numpy/scipy are its dependencies.
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10

# --- Sandbox UI (app.py) ---
# Pinned to a specific stable version known to work on HF Spaces (Python 3.10).
gradio==4.44.1
pandas>=2.0
