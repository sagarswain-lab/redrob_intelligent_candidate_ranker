# --- Core ranking pipeline (rank.py) ---
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10

# --- Sandbox UI (app.py) ---
# gradio is managed by HF Spaces via sdk_version in README.md (3.50.2).
# Do NOT add gradio or huggingface_hub here - they are pre-installed in the
# HF docker image and adding them causes version conflicts.
pandas>=2.0
